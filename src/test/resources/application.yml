quarkus:
  datasource:
    db-kind: h2
    username: sa
    password: sa
    devservices:
      enabled: false
    jdbc:
      url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
  hibernate-orm:
    database:
      generation: none
    sql-load-script: no-file
